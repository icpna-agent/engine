import { database } from '@db/connection.db';
import { message } from '@db/tables/message.table';

export async function seedMessages() {
  // No seed data for messages
  console.log('⏭️  Messages seed skipped');
}
