import { database } from '@db/connection.db';
import { chat } from '@db/tables/chat.table';

export async function seedChats() {
  // No seed data for chats
  console.log('⏭️  Chats seed skipped');
}
